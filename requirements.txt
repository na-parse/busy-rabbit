# busy-rabbit runtime dependencies.
# sqlite3 and tomllib are part of the Python standard library (3.11+).
Flask>=3.0,<4.0
# Used to generate the self-signed TLS pair in-process when use_https is on.
cryptography>=42.0
